"""
    Note :  با توجه به نوع داده ذخیره شده می توان به طریقه های مختلف فایل مورد نظر را باز کرد 


        do step by step base on https://docs.qgis.org/3.22/en/docs/training_manual/basic_map/preparation.html

        -The GeoPackage open format is a container that allows you to store GIS data (layers) in a single file. Unlike the ESRI Shapefile format (e.g. the protected_areas.shp dataset you loaded earlier), a single GeoPackage file can contain various data (both vector and raster data) in different coordinate reference systems, as well as tables without spatial information; all these features allow you to share data easily and avoid file duplication.

        -Loading vector data from a SpatiaLite Database with the Browser:
        Like GeoPackage, the SpatiaLite database format is an extension of the SQLite library. And adding a layer from a SpatiaLite provider follows the same rules as described above: Create the connection –> Enable it –> Add the layer(s).



    """
