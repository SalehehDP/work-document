"""
    Rather than plotting individual points as many graphics packages do, Datashader aggregates data onto a grid before plotting.
    
    the colour contour of wind speeds as well as the lines of grid segments are both rendered by Datashader.
    """
